--- a/runtime/hsa-runtime/image/image_manager_kv.h	2026-07-15 16:25:37.252022000 +0200
+++ b/runtime/hsa-runtime/image/image_manager_kv.h	2026-07-24 02:43:00.424438932 +0200
@@ -147,7 +147,8 @@
                              Image::TileMode tileMode,
                              size_t image_data_row_pitch,
                              size_t image_data_slice_pitch,
-                             ADDR_COMPUTE_SURFACE_INFO_OUTPUT& out) const;
+                             ADDR_COMPUTE_SURFACE_INFO_OUTPUT& out,
+                             uint32_t num_mipmap_levels = 1) const;
 
   size_t CalWorkingSizeBytes(hsa_ext_image_geometry_t geometry,
                              hsa_dim3_t size_pixel,
--- a/runtime/hsa-runtime/image/image_manager_kv.cpp	2026-07-24 02:43:00.408934105 +0200
+++ b/runtime/hsa-runtime/image/image_manager_kv.cpp	2026-07-24 02:43:00.422743145 +0200
@@ -238,7 +238,8 @@
       Image::TileMode::TILED : Image::TileMode::LINEAR;
   }
   if (!GetAddrlibSurfaceInfo(component, desc, tileMode,
-        image_data_row_pitch, image_data_slice_pitch, out)) {
+        image_data_row_pitch, image_data_slice_pitch, out,
+        num_mipmap_levels ? num_mipmap_levels : 1)) {
     return HSA_STATUS_ERROR;
   }
 
@@ -554,7 +555,17 @@
   }
   word2.bits.xy_min_filter = word2.bits.xy_mag_filter;
   word2.bits.z_filter = SQ_TEX_Z_FILTER_NONE;
-  word2.bits.mip_filter = SQ_TEX_MIP_FILTER_NONE;
+  switch (sampler_descriptor.mipmap_filter_mode) {
+    case HSA_EXT_SAMPLER_FILTER_MODE_NEAREST:
+      word2.bits.mip_filter = static_cast<int>(SQ_TEX_MIP_FILTER_POINT);
+      break;
+    case HSA_EXT_SAMPLER_FILTER_MODE_LINEAR:
+      word2.bits.mip_filter = static_cast<int>(SQ_TEX_MIP_FILTER_LINEAR);
+      break;
+    default:
+      word2.bits.mip_filter = static_cast<int>(SQ_TEX_MIP_FILTER_NONE);
+      break;
+  }
 
   word3.u32_all = 0;
 
@@ -725,13 +736,179 @@
 }
 
 hsa_status_t ImageManagerKv::PopulateMipmapSrd(MipmappedArray& mipmap) const {
-  // Kv (GFX8) architecture does not support mipmaps
-  return (hsa_status_t)HSA_EXT_STATUS_ERROR_IMAGE_FORMAT_UNSUPPORTED;
+  // GFX8 (CI/VI/Polaris) supports mip chains via BASE_LEVEL/LAST_LEVEL in
+  // SQ_IMG_RSRC_WORD3. Restore path for OpenMandriva testing (was stubbed).
+  ImageProperty mipmap_prop =
+      ImageLut().MapFormat(mipmap.desc.format, mipmap.desc.geometry);
+  if (mipmap_prop.cap == HSA_EXT_IMAGE_CAPABILITY_NOT_SUPPORTED ||
+      mipmap_prop.element_size == 0) {
+    return (hsa_status_t)HSA_EXT_STATUS_ERROR_IMAGE_FORMAT_UNSUPPORTED;
+  }
+  if (mipmap.num_levels < 1) {
+    return HSA_STATUS_ERROR_INVALID_ARGUMENT;
+  }
+
+  bool atc_access = true;
+  uint32_t mtype = mtype_;
+  const void* mipmap_data_addr = mipmap.data;
+
+  if (IsLocalMemory(mipmap.data)) {
+    atc_access = false;
+    mtype = 1;
+    mipmap_data_addr = reinterpret_cast<const void*>(
+        reinterpret_cast<uintptr_t>(mipmap.data) - local_memory_base_address_);
+  }
+
+  if (mipmap.desc.geometry == HSA_EXT_IMAGE_GEOMETRY_1DB) {
+    // Buffer resources are single-level only.
+    if (mipmap.num_levels != 1) {
+      return (hsa_status_t)HSA_EXT_STATUS_ERROR_IMAGE_FORMAT_UNSUPPORTED;
+    }
+    SQ_BUF_RSRC_WORD0 word0;
+    SQ_BUF_RSRC_WORD1 word1;
+    SQ_BUF_RSRC_WORD2 word2;
+    SQ_BUF_RSRC_WORD3 word3;
+
+    word0.u32_all = 0;
+    word0.bits.base_address = PtrLow32(mipmap_data_addr);
+
+    word1.u32_all = 0;
+    word1.bits.base_address_hi = PtrHigh32(mipmap_data_addr);
+    word1.bits.stride = mipmap_prop.element_size;
+    word1.bits.swizzle_enable = false;
+    word1.bits.cache_swizzle = false;
+
+    uint32_t major_ver = MajorVerFromDevID(chip_id_);
+    word2.bits.num_records = (major_ver < 8)
+                                 ? mipmap.desc.width
+                                 : mipmap.desc.width * mipmap_prop.element_size;
+
+    const Swizzle swizzle = ImageLut().MapSwizzle(mipmap.desc.format.channel_order);
+    word3.u32_all = 0;
+    word3.bits.dst_sel_x = swizzle.x;
+    word3.bits.dst_sel_y = swizzle.y;
+    word3.bits.dst_sel_z = swizzle.z;
+    word3.bits.dst_sel_w = swizzle.w;
+    word3.bits.num_format = mipmap_prop.data_type;
+    word3.bits.data_format = mipmap_prop.data_format;
+    word3.bits.atc = atc_access;
+    word3.bits.element_size = mipmap_prop.element_size;
+    word3.bits.type = ImageLut().MapGeometry(mipmap.desc.geometry);
+    word3.bits.mtype = mtype;
+
+    mipmap.srd[0] = word0.u32_all;
+    mipmap.srd[1] = word1.u32_all;
+    mipmap.srd[2] = word2.u32_all;
+    mipmap.srd[3] = word3.u32_all;
+
+    mipmap.row_pitch = mipmap.desc.width * mipmap_prop.element_size;
+    mipmap.slice_pitch = mipmap.row_pitch;
+    mipmap.size = mipmap.row_pitch;
+  } else {
+    SQ_IMG_RSRC_WORD0 word0;
+    SQ_IMG_RSRC_WORD1 word1;
+    SQ_IMG_RSRC_WORD2 word2;
+    SQ_IMG_RSRC_WORD3 word3;
+    SQ_IMG_RSRC_WORD4 word4;
+    SQ_IMG_RSRC_WORD5 word5;
+    SQ_IMG_RSRC_WORD6 word6;
+    SQ_IMG_RSRC_WORD7 word7;
+
+    ADDR_COMPUTE_SURFACE_INFO_OUTPUT out = {0};
+    if (!GetAddrlibSurfaceInfo(mipmap.component, mipmap.desc, mipmap.tile_mode,
+                               mipmap.row_pitch, mipmap.slice_pitch, out,
+                               mipmap.num_levels)) {
+      return HSA_STATUS_ERROR;
+    }
+    mipmap.addr_output.addr1 = out;
+    mipmap.size = out.surfSize;
+
+    assert((out.bpp / 8) == mipmap_prop.element_size);
+
+    const size_t row_pitch_size = out.pitch * mipmap_prop.element_size;
+
+    word0.bits.base_address = PtrLow40Shift8(mipmap_data_addr);
+
+    word1.u32_all = 0;
+    word1.bits.base_address_hi = PtrHigh64Shift40(mipmap_data_addr);
+    word1.bits.min_lod = 0;
+    word1.bits.data_format = mipmap_prop.data_format;
+    word1.bits.num_format = mipmap_prop.data_type;
+    word1.bits.mtype = mtype;
+
+    word2.u32_all = 0;
+    word2.bits.width = mipmap.desc.width - 1;
+    word2.bits.height =
+        (mipmap.desc.height == 0) ? 0 : (mipmap.desc.height - 1);
+    word2.bits.perf_mod = 0;
+    word2.bits.interlaced = 0;
+
+    const Swizzle swizzle = ImageLut().MapSwizzle(mipmap.desc.format.channel_order);
+    word3.u32_all = 0;
+    word3.bits.dst_sel_x = swizzle.x;
+    word3.bits.dst_sel_y = swizzle.y;
+    word3.bits.dst_sel_z = swizzle.z;
+    word3.bits.dst_sel_w = swizzle.w;
+    word3.bits.base_level = 0;
+    word3.bits.last_level = mipmap.num_levels - 1;
+    word3.bits.tiling_index = out.tileIndex;
+    word3.bits.pow2_pad =
+        (IsPowerOfTwo(row_pitch_size) && IsPowerOfTwo(mipmap.desc.height)) ? 1
+                                                                            : 0;
+    // SI+ requires pow2 pad for mip chains (addrlib pow2Pad flag).
+    if (mipmap.num_levels > 1) word3.bits.pow2_pad = 1;
+    word3.bits.type = ImageLut().MapGeometry(mipmap.desc.geometry);
+    word3.bits.atc = atc_access;
+
+    const bool mipmap_array =
+        (mipmap.desc.geometry == HSA_EXT_IMAGE_GEOMETRY_1DA ||
+         mipmap.desc.geometry == HSA_EXT_IMAGE_GEOMETRY_2DA ||
+         mipmap.desc.geometry == HSA_EXT_IMAGE_GEOMETRY_2DADEPTH);
+    const bool mipmap_3d = (mipmap.desc.geometry == HSA_EXT_IMAGE_GEOMETRY_3D);
+
+    word4.u32_all = 0;
+    word4.bits.depth =
+        (mipmap_array)
+            ? std::max(mipmap.desc.array_size, static_cast<size_t>(1)) - 1
+            : (mipmap_3d) ? mipmap.desc.depth - 1 : 0;
+    word4.bits.pitch = out.pitch - 1;
+
+    word5.u32_all = 0;
+    word5.bits.last_array =
+        (mipmap_array)
+            ? (std::max(mipmap.desc.array_size, static_cast<size_t>(1)) - 1)
+            : 0;
+
+    word6.u32_all = 0;
+    word7.u32_all = 0;
+
+    mipmap.srd[0] = word0.u32_all;
+    mipmap.srd[1] = word1.u32_all;
+    mipmap.srd[2] = word2.u32_all;
+    mipmap.srd[3] = word3.u32_all;
+    mipmap.srd[4] = word4.u32_all;
+    mipmap.srd[5] = word5.u32_all;
+    mipmap.srd[6] = word6.u32_all;
+    mipmap.srd[7] = word7.u32_all;
+
+    mipmap.row_pitch = row_pitch_size;
+    mipmap.slice_pitch = out.sliceSize;
+  }
+
+  mipmap.srd[8] = mipmap.desc.format.channel_type;
+  mipmap.srd[9] = mipmap.desc.format.channel_order;
+  mipmap.srd[10] = static_cast<uint32_t>(mipmap.desc.width);
+  mipmap.srd[11] = mipmap.num_levels;
+
+  return HSA_STATUS_SUCCESS;
 }
 
-hsa_status_t ImageManagerKv::PopulateMipmapSrd(MipmappedArray& mipmap_array, const metadata_amd_t* desc) const {
-  // Kv (GFX8) architecture does not support mipmaps
-  return (hsa_status_t)HSA_EXT_STATUS_ERROR_IMAGE_FORMAT_UNSUPPORTED;
+hsa_status_t ImageManagerKv::PopulateMipmapSrd(MipmappedArray& mipmap_array,
+                                               const metadata_amd_t* desc) const {
+  // Prefer descriptor-free path; metadata layout for CI/VI mip chains is
+  // uncommon and the live path builds the SRD from addrlib.
+  (void)desc;
+  return PopulateMipmapSrd(mipmap_array);
 }
 
 void ImageManagerKv::printSRDDetailed(const uint32_t* srd) const {
@@ -875,9 +1052,19 @@
     MipmappedArray& level_view,
     const MipmappedArray& mipmap_array,
     uint32_t mip_level) const {
+  if (mip_level >= mipmap_array.num_levels) {
+    return HSA_STATUS_ERROR_INVALID_ARGUMENT;
+  }
+  // Copy parent (srd is a fixed array — deep copy of the descriptor words).
+  level_view = mipmap_array;
 
-  // Mip level views not supported on GFX8 hardware
-  return HSA_STATUS_ERROR_NOT_INITIALIZED;
+  // GFX8: WORD3 carries BASE_LEVEL/LAST_LEVEL (same as GFX9+ for this purpose).
+  SQ_IMG_RSRC_WORD3* word3 =
+      reinterpret_cast<SQ_IMG_RSRC_WORD3*>(&level_view.srd[3]);
+  word3->bits.base_level = mip_level;
+  word3->bits.last_level = mip_level;
+
+  return HSA_STATUS_SUCCESS;
 }
 
 hsa_status_t ImageManagerKv::GetLocalMemoryRegion(hsa_region_t region,
@@ -984,7 +1171,8 @@
     Image::TileMode tileMode,
     size_t image_data_row_pitch,
     size_t image_data_slice_pitch,
-    ADDR_COMPUTE_SURFACE_INFO_OUTPUT& out) const {
+    ADDR_COMPUTE_SURFACE_INFO_OUTPUT& out,
+    uint32_t num_mipmap_levels) const {
   const ImageProperty image_prop =
       GetImageProperty(component, desc.format, desc.geometry);
 
@@ -1110,6 +1298,9 @@
   // Addrlib v1 (SI/CI/VI) rejects 0 height/slices for 1D/buffer textures.
   in.height = (height == 0) ? 1 : height;
   in.numSlices = (num_slice == 0) ? 1 : num_slice;
+  in.numMipLevels = (num_mipmap_levels == 0) ? 1 : num_mipmap_levels;
+  // SI+: pow2 pad required for mip chains (incl. level 0 when mipped).
+  in.flags.pow2Pad = (in.numMipLevels > 1) ? 1 : 0;
   in.flags.texture = 1;
   in.flags.noStencil = 1;
   in.flags.opt4Space = 0;
